<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/music_item_background"
    android:layout_marginBottom="8dp"
    android:padding="12dp">

    <!-- Image miniature de la musique (gradient coloré) -->
    <ImageView
        android:id="@+id/musicThumbnail"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/gradient_background"
        android:contentDescription="Miniature musique" />

    <!-- Titre de la musique -->
    <TextView
        android:id="@+id/musicTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="15dp"
        android:layout_toEndOf="@id/musicThumbnail"
        android:layout_toStartOf="@id/bookmarkIcon"
        android:text="Titre 1"
        android:textColor="#000000"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Icône bookmark (marque-page) -->
    <ImageView
        android:id="@+id/bookmarkIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="40dp"
        android:src="@android:drawable/btn_star_big_off"
        android:contentDescription="Bookmark"
        android:tint="#00BCD4" />

    <!-- Menu 3 points -->
    <ImageView
        android:id="@+id/menuIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:src="@android:drawable/ic_menu_more"
        android:contentDescription="Menu" />

</RelativeLayout>